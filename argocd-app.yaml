apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: milestone-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/iDqrks/BV_Milestone2.git
    targetRevision: main
    path: k8s
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: milestone-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
